<Window x:Class="WpfSkeleton.CreateWorkDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Work" 
        MinHeight="450" MinWidth="550" 
        Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}"
        SizeToContent="Manual">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="TitleText" Grid.Row="0" Text="Create Work" 
                       FontSize="20" FontWeight="Bold" Margin="0,0,0,20"
                       Foreground="{DynamicResource ForegroundBrush}"
                       TextWrapping="Wrap"/>

            <StackPanel Grid.Row="1" Margin="0,0,0,15">
                <TextBlock x:Name="WorkNameLabel" Text="Work name:" Margin="0,0,0,5"
                           Foreground="{DynamicResource ForegroundBrush}"/>
                <TextBox x:Name="WorkNameTextBox" MinHeight="30" Padding="5" 
                         TextWrapping="NoWrap" VerticalContentAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Margin="0,0,0,15">
                <TextBlock x:Name="SourcePathLabel" Text="Source path:" Margin="0,0,0,5"
                           Foreground="{DynamicResource ForegroundBrush}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MinWidth="200"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="SourcePathTextBox" Grid.Column="0" MinHeight="30" 
                             Padding="5" Margin="0,0,5,0" VerticalContentAlignment="Center"/>
                    <Button x:Name="BrowseSourceButton" Grid.Column="1" Content="Browse..." 
                            MinWidth="90" Height="30" Click="BrowseSource_Click" Padding="10,0"/>
                </Grid>
            </StackPanel>

            <StackPanel Grid.Row="3" Margin="0,0,0,15">
                <TextBlock x:Name="DestinationPathLabel" Text="Destination path:" Margin="0,0,0,5"
                           Foreground="{DynamicResource ForegroundBrush}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MinWidth="200"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="DestinationPathTextBox" Grid.Column="0" MinHeight="30" 
                             Padding="5" Margin="0,0,5,0" VerticalContentAlignment="Center"/>
                    <Button x:Name="BrowseDestinationButton" Grid.Column="1" Content="Browse..." 
                            MinWidth="90" Height="30" Click="BrowseDestination_Click" Padding="10,0"/>
                </Grid>
            </StackPanel>

            <StackPanel Grid.Row="4" Margin="0,0,0,15">
                <TextBlock x:Name="SaveTypeLabel" Text="Save type:" Margin="0,0,0,5"
                           Foreground="{DynamicResource ForegroundBrush}"/>
                <ComboBox x:Name="SaveTypeComboBox" MinHeight="30" VerticalContentAlignment="Center">
                    <ComboBoxItem x:Name="CompleteItem" Content="Complete" IsSelected="True"/>
                    <ComboBoxItem x:Name="IncrementalItem" Content="Incremental"/>
                </ComboBox>
            </StackPanel>

            <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" 
                        Margin="0,15,0,0">
                <Button x:Name="CreateButton" Content="Create" 
                        MinWidth="100" Height="35" Margin="0,0,10,0" 
                        Click="Create_Click" Padding="15,0"/>
                <Button x:Name="CancelButton" Content="Cancel" 
                        MinWidth="100" Height="35" 
                        Click="Cancel_Click" Padding="15,0"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Window>
